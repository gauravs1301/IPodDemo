import React from "react";
import Ipod from "./Ipod"; // Importing the Ipod component from the "./Ipod" file.

function App() {
  return (
    <div className="App">
      <Ipod />
      {/* Rendering the Ipod component inside the App component. */}
    </div>
  );
}

export default App; //Exporting the App component as the default export.
